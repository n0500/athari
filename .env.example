# Example only. Do not put real secrets here.
VITE_APP_NAME=Athari

# Public client configuration may be added later when the architecture is approved.
# Secret AI keys must NEVER use a VITE_ prefix or be exposed to the browser.
